# ピットイン・クイズレーサー 引き継ぎメモ（2026-08-20）

## いま公開されているもの

- 公開URL: https://pit-in-quiz-racer.briny-bow-3704.chatgpt.site
- レースデモ: https://pit-in-quiz-racer.briny-bow-3704.chatgpt.site/demo
- 公開設定は「所有者のみ」の非公開です。公開範囲を変える時は、Codex の Sites 公開設定で変更します。

## 今日作った内容

子供・大人・家族で遊べる、車の整備クイズとレースを組み合わせたアプリの土台を作りました。

- 最初のページは、文章「クイズに答えて車を育てよう。」とスタートボタンだけに整理しました。
- 次のページで車を選び、各車の得意・不得意を読めるようにしました。
- 初級・中級・上級の3クラスを用意しました。選んだ車の改造性能は、次のクラスへ引き継げます。
- 4択クイズを1問ずつ回答し、正解数に応じてタイヤ、ウイング、車高、エンジンを改造できる流れを作りました。
- クラス終了時には、レースを始めるか、次のクラスへ進むかを選べます。
- 同じ車の性能で走れるレースは3回までです。3回目の後は10桁の引き継ぎコードを発行し、別日に入力すると選んだ車・改造・クラスを引き継いで再開できます。
- メイン車3台と対戦車4台のオリジナル画像を追加しました。
- 「幻のレジェンドカー」用に、銀色で汚れた古い車の画像も追加しました（ゲーム内の選択・解放処理は未着手です）。
- 30秒で完走するレースデモを `/demo` に追加しました。5台が走り、残り時間・順位・優勝車が表示されます。

## 主な車と設定

| 区分 | 名前 | 方向性 |
| --- | --- | --- |
| 初級 | ポコ | 軽四。軽量で小回りが利き、市街地コースが得意。高出力エンジンは積めない。 |
| 中級 | ノア | 小型・普通車。扱いやすいバランス型。 |
| 上級 | レイ | スポーツカー。高速コースが得意。 |
| 対戦相手 | ミント / サンダー / モモ / ギン | それぞれコーナー、直線、安定、総合力を担当。 |
| 今後追加 | レグルス | 銀色で傷んだ幻のレジェンドカー。全カテゴリー制覇とパーツ交換で復活する設定。 |

## 次にやるとよいこと

1. レグルスを実際に選べる車へ追加し、「全カテゴリー制覇後に解放」の条件を実装する。
2. 240問（4ジャンル × 初級・中級・上級 × 各20問）を作成する。現状は流れを確認するための問題のみです。
3. クイズ結果に応じた改造ポイントと、レースの速さの関係を見直す。
4. 必要であれば、通常のレース画面にも今回の30秒デモの演出を取り込む。

## 主なファイル

- `app/page.tsx` : 通常のクイズ、車選択、改造、レース、引き継ぎコードの画面
- `app/demo/page.tsx` : 30秒のレースデモ
- `app/globals.css` : 全画面の見た目とレースデモのコース表示
- `public/character-lineup.png` : メイン車3台の画像
- `public/rival-lineup.png` : 対戦車4台の画像
- `public/legend-car.png` : レジェンドカーの画像
- `app/api/garage-save/route.ts` : 引き継ぎコードを保存・読込する処理
- `db/schema.ts` / `drizzle/0000_keen_johnny_blaze.sql` : 引き継ぎデータ用の保存先定義
- `.openai/hosting.json` : 公開サービス用の設定。データ保存先 `DB` を指定しています。

## 確認済みのこと

- 2026-08-20にビルド成功を確認済みです。
- `/`, `/demo`, `/api/garage-save` がビルド出力に含まれることを確認済みです。
- レースデモを含む最新版を公開済みです。

Windows では `npm run build` の環境変数の書式が合わないことがあります。その場合は、プロジェクト直下で次を実行してください。

```powershell
cmd /c "set WRANGLER_LOG_PATH=.wrangler/wrangler.log&& node_modules\.bin\vinext.cmd build"
```

## Gitの状態

- 最新コミット: `bef4c9c Add thirty second race demo`
- その前: `b4ebdcc Split welcome and vehicle selection flow`
- 未追跡ファイル: `dev.log` と `package-stage*` フォルダーがあります。公開用の一時作業フォルダーなので、アプリ本体の修正対象ではありません。削除する場合は、公開用アーカイブが不要か確認してからにしてください。
- 公開用アーカイブは `outputs/` にあります。最新版の通常アーカイブは `outputs/pit-in-quiz-racer-v7.tgz`、デモ作成時のアーカイブは `outputs/race-demo.tgz` です。

## 公開時の注意

- ソースを変更しただけでは、公開サイトは更新されません。
- Codex の Sites 機能で新しいバージョンを保存し、非公開で再公開してください。
- 引き継ぎコードには個人情報を入れない設計です。選択車、改造、クラス、レース回数だけを保存します。
