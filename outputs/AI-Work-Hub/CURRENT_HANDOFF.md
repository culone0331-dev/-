# AI Work Hub 現在の引き継ぎ

更新日: 2026-09-19

## 共通の起点

- 確認済み統合ブランチ: `integration/ai-work-hub`
- この案件の作業開始時は、Codex・Claude Codeとも必ずこのブランチを取得する。
- GitHubの既定ブランチはAI Work Hub以外の内容を含む可能性があるため、この案件の統合先に使わない。

## Codex側のレビュー待ち変更

- ブランチ: `codex/local-llm-job-queue`
- 最新コミット: `effd8a6`
- 内容:
  - culone-serverとAI2へのローカルLLM依頼をバックグラウンドの待機列へ変更
  - 同一推論機では一件ずつ、二台には並行して実行
  - 案件画面へ待機中・確認中・完了・失敗を表示
  - 通常レビューを4Kコンテキストに固定し、巨大な既定コンテキストで統合メモリを消費しない
  - 共同作業規約、GitHubプルリクエスト用テンプレート、構文確認ワークフローを追加

## 実施済み確認

- Pythonサーバーとブラウザー側JavaScriptの構文確認は成功。
- ローカルLLM依頼が待機中から確認中へ遷移することを確認。
- 旧方式の既定262Kコンテキストが57GBを消費することを発見し、4K制限を追加。
- 旧8787稼働版とSQLiteデータは変更していない。

## Claude Codeへのレビュー依頼

1. `integration/ai-work-hub` を基準に `codex/local-llm-job-queue` の差分を確認する。
2. 同一機一件・二機並行のロック方式、ジョブ失敗時の表示、案件への回答保存を確認する。
3. 4K制限で、ローカルLLMの回答が案件へ完了保存される一連の実機確認をする。
4. 問題がなければ、変更を `integration/ai-work-hub` へ取り込む提案を出す。問題があれば、`claude/` ブランチで修正し、ここを更新する。

## 未確認点

- NASの共有パスと認証方法は未登録。バックアップ自動化はまだ開始しない。
- 外部AIサービスへの自動投稿は対象外。Hubは引き継ぎ文を生成・記録し、利用者が投稿する。
